
user  nginx;
worker_processes  auto;

error_log  /var/log/nginx/error.log notice;
pid        /run/nginx.pid;


events {
    worker_connections  1024;
}


http {
    include       /etc/nginx/mime.types;
    default_type  application/octet-stream;

    log_format  main  '$remote_addr - $remote_user [$time_local] "$request" '
                      '$status $body_bytes_sent "$http_referer" '
                      '"$http_user_agent" "$http_x_forwarded_for"';

    access_log  /var/log/nginx/access.log  main;

    sendfile        on;
    #tcp_nopush     on;

    keepalive_timeout  65;
    client_body_buffer_size 1M;

    #gzip  on;

    map $http_upgrade $connection_upgrade {
            default upgrade;
            ''      close;
    }

    server {
        listen       6001;
        server_name  localhost;

        location /health {
            proxy_pass http://standalone-mare-server-1:6000;
            proxy_http_version 1.1;
            proxy_set_header Host $http_host;
            proxy_set_header Upgrade $http_upgrade;
            proxy_set_header Connection $connection_upgrade;
            proxy_set_header X-Forwarded-For $remote_addr;
        }

        location /auth/ {
            proxy_pass http://standalone-mare-auth-1:8080;
            proxy_http_version 1.1;
            proxy_set_header CF-CONNECTING-IP $remote_addr;
            proxy_set_header X-Forwarded-Proto $scheme;
            proxy_set_header Host $host;
            proxy_set_header Connection "";
            proxy_buffering on;
        }

        # # Lock down this endpoint using e.g. IP whitelisting if possible
        # location /configuration/ {
        #     proxy_pass http://standalone-mare-server-1:6000;
        #     proxy_http_version 1.1;
        #     proxy_set_header CF-CONNECTING-IP $remote_addr;
        #     proxy_set_header X-Forwarded-Proto $scheme;
        #     proxy_set_header Host $host;
        #     proxy_set_header Connection "";
        #     proxy_buffering on;
        # }

        # # Server-to-server only
        # # Lock down this endpoint using e.g. IP whitelisting if possible
        # location /main/ {
        #     proxy_pass http://standalone-mare-server-1:6000;
        #     proxy_http_version 1.1;
        #     proxy_set_header CF-CONNECTING-IP $remote_addr;
        #     proxy_set_header X-Forwarded-Proto $scheme;
        #     proxy_set_header Host $host;
        #     proxy_set_header Connection "";
        #     proxy_buffering on;
        # }

        # Websocket path
        location = /mare {
            proxy_pass http://standalone-mare-server-1:6000/mare;
            proxy_http_version 1.1;
            proxy_set_header Upgrade $http_upgrade;
            proxy_set_header CF-CONNECTING-IP $remote_addr;
            proxy_set_header X-Forwarded-Proto $scheme;
            proxy_set_header Host $host;
            proxy_read_timeout 70;
            proxy_send_timeout 70;
            proxy_buffering off;
        }

        location = /mare/ {
            proxy_pass http://standalone-mare-server-1:6000;
            proxy_http_version 1.1;
            proxy_set_header Upgrade $http_upgrade;
            proxy_set_header CF-CONNECTING-IP $remote_addr;
            proxy_set_header X-Forwarded-Proto $scheme;
            proxy_set_header Host $host;
            proxy_read_timeout 70;
            proxy_send_timeout 70;
            proxy_buffering off;
        }

        location /files/ {
            proxy_pass http://standalone-mare-files-1:6200;
            proxy_http_version 1.1;
            proxy_set_header CF-CONNECTING-IP $remote_addr;
            proxy_set_header X-Forwarded-Proto $scheme;
            proxy_set_header Host $host;
            proxy_set_header Connection "";
            client_max_body_size 200M;
            proxy_read_timeout 1800;
            proxy_send_timeout 1800;
            proxy_connect_timeout 5;
            proxy_hide_header Cache-Control;
            add_header Cache-Control private;
        }

        # # Server-to-server only
        # # Lock down this endpoint using e.g. IP whitelisting if possible
        # location /dist/ {
        #     proxy_pass http://standalone-mare-files-1:6200;
        #     proxy_http_version 1.1;
        #     proxy_set_header CF-CONNECTING-IP $remote_addr;
        #     proxy_set_header X-Forwarded-Proto $scheme;
        #     proxy_set_header Host $host;
        #     proxy_set_header Connection "";
        #     client_max_body_size 200M;
        #     proxy_read_timeout 1800;
        #     proxy_send_timeout 1800;
        #     proxy_connect_timeout 5;
        #     proxy_hide_header Cache-Control;
        #     add_header Cache-Control "public, max-age=86400";
        # }

        location /cache/ {
            proxy_pass http://standalone-mare-files-1:6200;
            proxy_http_version 1.1;
            proxy_set_header CF-CONNECTING-IP $remote_addr;
            proxy_set_header X-Forwarded-Proto $scheme;
            proxy_set_header Host $host;
            proxy_set_header Connection "";
            client_max_body_size 200M;
            proxy_read_timeout 1800;
            proxy_send_timeout 1800;
            proxy_connect_timeout 5;
            proxy_hide_header Cache-Control;
            add_header Cache-Control private;
        }

        location /request/ {
            proxy_pass http://standalone-mare-files-1:6200;
            proxy_http_version 1.1;
            proxy_set_header CF-CONNECTING-IP $remote_addr;
            proxy_set_header X-Forwarded-Proto $scheme;
            proxy_set_header Host $host;
            proxy_set_header Connection "";
            client_max_body_size 200M;
            proxy_read_timeout 1800;
            proxy_send_timeout 1800;
            proxy_connect_timeout 5;
            proxy_hide_header Cache-Control;
            add_header Cache-Control private;
        }

        # location / {
        #  proxy_pass http://standalone-mare-server-1:6000;
        #     proxy_http_version 1.1;
        #     proxy_set_header Host $http_host;
        #     proxy_set_header Upgrade $http_upgrade;
        #     proxy_set_header Connection $connection_upgrade;
        #     proxy_set_header X-Forwarded-For $remote_addr;
        # }
    }
}
